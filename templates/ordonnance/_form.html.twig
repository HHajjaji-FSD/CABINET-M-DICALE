<div class="col-md-10 container-fluid">
    {{ form_start(form) }}
    <div class="card card-primary">

        <div class="card-header">
            <h3 class="card-title">Ajouter un Odronnace</h3>
        </div>

        <div class="card-body">

            {{ form_row(form.prixFinal) }}

            {{ form_row(form.radio) }}

            {{ form_row(form.medicaments) }}

            {#<input id="medicOrdo" name="medicOrdo" type="hidden"/>#}

        </div>


        <div class="card-footer">
            {{ form_widget(form.Save) }}
        </div>

    </div>
    {{ form_end(form) }}
</div>